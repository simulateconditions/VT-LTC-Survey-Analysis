1. Open terminal in folder with all of the code

2. source /opt/anaconda3/bin/activate
Note: this works if (base) comes in front of the entire line in the terminal

3. conda activate survey
Note: this works if (survey) comes in front of the entire line in the terminal

4. python gui_run.py

5. modify the page numbers at the bottom of new_pdf_download.py in order to acutally search all SNF and RCH pages. ALR is correct. Make sure to save the file after modifying the numbers.

